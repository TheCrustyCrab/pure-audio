[package]
name = "oscillator-web"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
oscillator = { path = "../oscillator" }
pure-audio-wasm = { path = "../../pure-audio-wasm"}
